/**
 * BroadcastPage - Broadcast Campaigns Management
 * Wraps the existing BroadcastPanel component
 */

import { BroadcastPanel } from '../components/admin/BroadcastPanel';

export const BroadcastPage = () => {
    return <BroadcastPanel />;
};
